# 安全政策

## 支持范围

安全修复优先覆盖当前 `main` 和最新正式版本。历史版本可能只获得升级建议。

## 私密报告漏洞

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告功能。不要为下列问题创建公开 Issue：

- API Key、令牌、密码、私钥或数据库凭据泄漏；
- 任意文件读取、路径穿越或跨任务数据访问；
- 只读 SQL 限制绕过或数据库写入；
- 公式注入、宏安全绕过或危险 VBA 生成；
- AI 白名单/本地安全校验绕过；
- 未经授权向外部模型发送客户单元格数据。

报告应包含版本、影响、脱敏复现步骤和建议修复。不要附真实客户数据或有效凭据。维护者会尽快确认收到报告，并在完成修复和发布后协调披露。

## 如果密钥已经泄漏

1. 立即到对应服务商撤销并重新生成密钥；仅从 Git 历史删除并不能使密钥失效。
2. 检查仓库历史、Issue、PR、Actions 日志和发布附件。
3. 启用 GitHub Secret Scanning 和 Push Protection。
4. 用新密钥更新本机 `.env` 或 Windows DPAPI 保险箱；不要再次提交。

## 项目安全边界

- 服务默认只绑定 `127.0.0.1`。
- `.env`、`user_data/`、`outputs/`、数据库和 Excel/CSV 默认禁止提交。
- DeepSeek API Key 只在服务端使用，不应进入前端、计划、日志或交付文件。
- AI 只能生成白名单计划；数据计算和文件操作由本地确定性代码执行。
- 财务、人事、审批和风险结论必须由具备相应权限的人复核。

发布前运行：

```bash
python scripts/check_secrets.py
python -m pytest -q
```
